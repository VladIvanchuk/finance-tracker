import { Account } from "@/schemas/Account";
import { Transaction } from "@/schemas/Transaction";
import { Category } from "./Category";
import { User } from "./User";

const schemas = [User, Account, Transaction, Category];

export default schemas;
